# Spectremon v4.2.1

Documentation-only patch:

- README: installer file tree now shows the bundled `execute-task` workflow, the plugin summary lists the hooks and workflow, and the Phase 4 approval wording reflects the structured `{passed, feedback}` verdict introduced in v4.2.0.
- Clarified that the installer does not modify `settings.json`, so the `specs/` protection hooks remain plugin-only.
